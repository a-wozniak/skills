{
  "$schema": "https://cloud.uipath.com/agent-schema/v1.1.0/agent-schema.json",
  "version": "1.1.0",
  "name": "{AGENT_NAME}",
  "description": "{AGENT_DESCRIPTION}",
  "engine": "basic-v2",
  "model": "gpt-4o-2024-11-20",
  "prompts": {
    "system": "You are a helpful assistant. Complete the user's task accurately and concisely. If you are missing information required to complete the task, ask a clarifying question before proceeding.",
    "user": "{{task}}\n\n{{#if context}}Additional context:\n{{context}}{{/if}}"
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "task": {
        "type": "string",
        "description": "The task or question for the agent to complete."
      },
      "context": {
        "type": "string",
        "description": "Optional background information or constraints relevant to the task."
      }
    },
    "required": ["task"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "result": {
        "type": "string",
        "description": "The agent's response or the outcome of the completed task."
      }
    },
    "required": ["result"]
  },
  "resources": [],
  "features": []
}
